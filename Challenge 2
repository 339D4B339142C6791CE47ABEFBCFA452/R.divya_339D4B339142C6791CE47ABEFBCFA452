# leap year or not

"""
year % 4==0&
year%100!=0/
year% 400==0

"""
def is leapyear(year):
if(year % 4==0 and year % 100!=0).year%400==0
return true
else:
return false

year=int ( input("Enter a year:"))

if is leapyear(year)
print({} is a leap year.". format (year))
else:
print ("{} is a nit a leapyear.".format(year))
